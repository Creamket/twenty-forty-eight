/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/twenty-forty-eight',
  output: 'export', // <=== enables static exports
  reactStrictMode: true,
}

export default nextConfig
